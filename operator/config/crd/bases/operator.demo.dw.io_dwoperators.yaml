---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.8.0
  creationTimestamp: null
  name: dwoperators.operator.demo.dw.io
spec:
  group: operator.demo.dw.io
  names:
    kind: DwOperator
    listKind: DwOperatorList
    plural: dwoperators
    singular: dwoperator
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: DwOperator is the Schema for the dwoperators API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: DwOperatorSpec defines the desired state of DwOperator
            properties:
              name:
                description: DeploymentName is the identity of the dwserver deployment
                type: string
              namespace:
                description: Namespace of the dw deployment, if omitted, the default
                  value is the default namespace
                type: string
              replicas:
                description: Replicas defines number of replicas for the dwserver
                format: int32
                type: integer
            required:
            - name
            - namespace
            - replicas
            type: object
          status:
            description: DwOperatorStatus defines the observed state of DwOperator
            properties:
              availability_zone:
                additionalProperties:
                  format: int32
                  type: integer
                type: object
              total_running:
                format: int32
                type: integer
              total_scheduled:
                format: int32
                type: integer
            required:
            - availability_zone
            - total_running
            - total_scheduled
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
